<div id="<%= dom_id post %>">

    <h2><%= post.title %></h2>
    <h4>posted by <%= post.user.email %></h4>
    <h6><%= pluralize(post.views, "views") %></h6>

  <p>
    <strong>Body:</strong>
    <%= post.body %>
  </p>

</div>
